# 상시 규칙

bonandgyeol-site 작업 시 항상 적용. 세션이 바뀌어도 이 파일이 기준입니다.

## 프로젝트

정적 사이트 4페이지(`index` `platform` `curriculum` `404`). 빌드 없음, 인라인 CSS/JS.
`main` push → GitHub Actions → Cloudflare Pages. **라이브 상태**(https://bonandgyeol.com).

Pages가 `.html`을 벗긴 주소로 308 리다이렉트합니다. 링크·canonical·sitemap은 `/platform` 형태로 씁니다.

---

## 금지선 (예외 없음)

| # | 금지 |
|---|---|
| 1 | 파일 삭제 · force push · 히스토리 변경 |
| 2 | 없는 실적 표기 — 수강생 수, 참여자 수, 발매 실적, 수상, 만족도 |
| 3 | 수강료 금액 · 개강일 · 정원 표기 (미확정) |
| 4 | 외부 이미지 CDN |
| 5 | 빌드 도구 · 프레임워크 도입 |
| 6 | 단일 HTML + 인라인 CSS/JS 원칙 위반 |
| 7 | 폼 편집 URL · 토큰 커밋 |
| 8 | 로컬만 보고 배포 상태 판단 |

3번은 **금액·날짜·정원에만** 적용됩니다. 수수료율과 케어 수치는 확정값이라 공개 가능합니다.

---

## 확정 결정 (다시 묻지 않음)

| 항목 | 결정 |
|---|---|
| og:image | 자체 호스팅 PNG 1장 허용 (`og.png`) |
| 폰트 셀프호스팅 | 보류 — 카피가 자주 바뀌어 글자 누락 위험 |
| PSI / Lighthouse | 큐에서 제외 (node 없음, PSI 쿼터 초과) |
| 로고 대비 4.40 | WCAG 로고타입 예외로 유지. 팔레트 변경 금지 |
| 대메뉴 | 3개 고정 — fakeREAL · 교육 · 소개 |
| 「본과 결」 사전 | 소개(하단)에 위치 |
| CARE 모듈 | 두 트랙 공통, 전 과정 병행 (주차 고정 표기 안 함) |

### 공개 가능한 확정 수치

매칭 수수료 0% · 매니지먼트 수수료 10~25% · 월 1회 세션 · 연 4주 안식 · 실전 6주 상한 · 페르소나 자율 · 12주 과정 · Suno·DAW 2트랙

---

## 카피 규칙

**한 자리에 하나만** — 헤드는 캐치(짧게, 정보 없어도 됨), 본문은 설명(사실만, 멋 없어도 됨). 섞지 않습니다.

**금지**: 비유·은유(자란다/무늬/뿌리/결실/문은 하나) · "A가 아니라 B" 대구 · 회사가 주어인 헤드 · 형용사 나열 · "~입니다" 반복

**필수**: 헤드 주어는 사용자 · 명사구 또는 사실 진술 · 숫자 우선

각 문장에 물을 것:
1. 이 문장이 없으면 무슨 정보가 사라지나? (답이 없으면 삭제)
2. 캐치인가 설명인가? (둘 다면 분리)
3. 주어가 회사인가 사용자인가?
4. 비유가 있나?

---

## 디자인 규칙

| 항목 | 값 |
|---|---|
| 팔레트 | `#5C7348` `#22231F` `#EDEBE3` (변경 금지) |
| 서체 | Cormorant Garamond · Gowun Batang · IBM Plex Sans KR |
| 접근성 | 44px+ 타깃 · focus-visible · 대비 4.5:1 · prefers-reduced-motion · Escape로 메뉴 닫기 |

---

## 구조상 함정 (실제로 사고가 났던 것)

| 항목 | 내용 |
|---|---|
| `.wrap` + 다른 클래스 | 같은 특정도면 뒤 규칙이 이깁니다. `.dict{padding:90px 0}`이 `.wrap`의 좌우 24px을 0으로 덮어써 소개 섹션이 화면 끝에 붙었습니다 |
| `html.js` 게이팅 | `.reveal`·모바일 네비는 기본이 **보이는** 상태. `html.js`가 붙을 때만 애니메이션·드롭다운이 켜집니다. 기본을 숨김으로 바꾸면 JS 없는 환경에서 백지가 됩니다 |
| `[hidden]` | `.nav-links a`의 `display:flex`가 `hidden`을 덮어써서 `[hidden]{display:none !important}`가 필요합니다 |
| Analytics + CSP | beacon은 HTML에만 주입됩니다. CSP에 cloudflareinsights가 없으면 조용히 차단돼 수집이 0건이 됩니다 |
| 한글 폰트 서브셋 | Google Fonts는 한글을 글자 단위로 쪼갭니다. 폰트를 직접 쓸 때 글자마다 맞는 서브셋을 받지 않으면 두부(□)가 됩니다 |
| 링크 문구 중복 | 같은 문구가 다른 목적지를 가리키면 `.sr-only`로 구분합니다 |

---

## 검증 절차

**세션 끝에 반드시 실행**하고 결과를 REPORT 3절에 기록:

```bash
curl -sI https://bonandgyeol.com | head -1
curl -sI https://bonandgyeol.com/platform | head -1
curl -sI https://bonandgyeol.com/curriculum | head -1
curl -sI https://bonandgyeol.com/sitemap.xml | head -1
```

| 대상 | 방법 |
|---|---|
| 배포 반영 | curl로 응답 본문에 변경 내용이 있는지 (30~60초 소요) |
| 스크립트 주입·CSP 차단 | **브라우저 콘솔** — curl로는 안 보입니다 |
| 링크·앵커 | 전수 스크립트 (확장자 없는 경로 해석 포함) |
| 접근성 | 브라우저 DOM 검사 + 대비 계산. axe는 node가 없어 사용 불가 |
| 반응형 | 320 / 640 / 768 / 1440px 계산값 |
| JS 비활성 | `html.js` 클래스를 제거해 재현 |
| 텍스트 간격 | line-height 1.5 / letter-spacing .12em / word-spacing .16em 강제 |
| 파일 크기·전송량 | **실측만.** 기억으로 쓰지 않습니다 |

측정 순서 주의: 상태를 되돌린 뒤 값을 읽으면 잘못된 결과가 나옵니다. 판정은 복구 **전에** 계산합니다.
`transition`이 있는 속성은 전이 완료 후 읽습니다.

---

## 작업 방식

- 항목마다 커밋 분리, 배치마다 push
- 막히면 기록하고 다음 항목으로. 멈추지 않습니다
- 결정이 필요하면 REPORT 2절에 적고 다음으로 넘어갑니다
- 이미 완료된 항목은 재작업하지 않고 검증만 합니다

## REPORT.md 형식

표 중심·산문 최소. **안 한 것·실패한 것을 먼저.** "완료"는 검증 결과가 있을 때만.

1. 요약 (`# 항목 상태 커밋`, 상태는 ✅⚠️❌⏸️, ❌·⏸️는 원인 1줄)
2. **두호 결정 필요** ← 상단 배치 (아침 5분에 여기만 봅니다)
3. 검증 (방법 명시, 추정은 `(추정)` 표기)
4. 변경 상세
5. 미해결·리스크 (못 잡은 것 포함, "없음"으로 비우지 않음)
6. 다음 세션 큐 (3~6개, 선행조건 명시)
