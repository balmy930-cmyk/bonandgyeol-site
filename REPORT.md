# 작업 보고서 — 2026-08-11

배포 커밋: `edc4d98` 이후 · 저장소 clean · **큐 소진**

---

## 1. 두호 결정 필요

| # | 항목 | 선택지 | 권장 | 근거 |
|---|---|---|---|---|
| 1 | 3G 실측 | (a) 스로틀 가능한 환경에서 재측정 (b) 추정치로 유지 | **(b)** | 브라우저 스로틀 API가 없어 실측 불가. 추정으로는 폰트 전량 3G 느림 ~28초 (3절) |
| 2 | 폰트 부하 | (a) 서브셋 전환 (b) 굵기 축소 (c) 유지 | **(c) 유지** | 확정 결정대로 보류. 다만 폰트가 총 전송량의 99%로 유일한 병목 |
| 3 | curriculum DAW W6 | (a) 「보컬 디렉션」 확정 (b) 다른 안 | **(a)** | 지시대로 복원 완료. W3 탑라인과 역할이 달라 중복 아님 |
| 4 | og.png 디자인 | (a) 현재(워드마크만) 유지 (b) 문구 추가 | **(a)** | 공유 카드에서 작게 표시되므로 워드마크만이 읽힘 |

---

## 2. 요약

| # | 항목 | 상태 | 커밋 |
|---|---|---|---|
| E3 | 3G 시뮬레이션 렌더 | ❌ 실패 — 브라우저 스로틀 수단 없음. 추정치로 대체 | — |
| A3 | MEDIA 빈 배열 숨김 실측 | ⚠️ 부분 — 숨김은 확인, 항목 있는 상태는 주소 미확보로 미검증 | — |
| C2 | Critical CSS 분리 | ⏸️ 보류 — 지시대로 계산만 | — |
| A1 | DAW W6 「보컬 디렉션」 복원 | ✅ | `4b62c45` |
| A2 | og.png 제작 + 3페이지 메타 | ✅ | `fe0dc61` |
| B1 | 키보드 순회 (Escape 추가) | ✅ | `c922de0` |
| B2 | 스크린리더 (3건 수정) | ✅ | `c922de0` |
| B3 | 확대 200%/400% 리플로우 | ✅ | — |
| B4 | 텍스트 간격 내성 | ✅ | — |
| C1 | 전송량·요청 수 실측 | ✅ | — |
| C3 | 폰트 로딩 전략 | ✅ | — |
| C4 | LCP 요소 특정 | ✅ | — |
| D1~D4 | 카피·실적·가격·용어 재검사 | ✅ 위반 0 | — |
| D5 | JSON-LD 일치 | ✅ | `38cc347` |
| E1 | 링크 전수 상태코드 | ✅ | — |
| E2 | JS 비활성 (2건 수정) | ✅ | `8196b82` → `edc4d98` |
| E4 | 콘솔 0건 | ✅ | — |
| E5 | HTML 유효성 | ✅ | — |
| F1 | README 갱신 | ✅ | 이 커밋 |
| F2 | CLAUDE.md 생성 | ✅ | 이 커밋 |
| F3 | REPORT 갱신 | ✅ | 이 커밋 |

### 이번 세션에 잡은 결함 3건

| # | 결함 | 영향 |
|---|---|---|
| 1 | **JS 없으면 본문이 백지** | `.reveal{opacity:0}`이 초기값이고 JS가 드러내는 구조. index 30·platform 14·curriculum 22개 요소가 투명 상태로 남음 |
| 2 | **JS 없으면 모바일 네비 도달 불가** | 820px 미만에서 `.nav-links{display:none}`, `.open`을 JS가 붙임 |
| 3 | **소개 섹션 좌우 여백 0** | `.dict{padding:90px 0}`이 `.wrap`의 `0 24px`를 덮어씀. 1020px 미만에서 글자가 화면 끝에 붙음 |

3번은 이번 세션 시작 전 「사이트 보여줘」 요청으로 스크린샷을 보다가 발견했습니다. 자동 검사는 "화면 밖으로 나감"만 봤기 때문에 통과했습니다.

---

## 3. 검증

### 필수 확인 (curl, 세션 종료 시점)

| 항목 | 방법 | 결과 |
|---|---|---|
| `bonandgyeol.com` | curl -sI | `HTTP/1.1 200 OK` |
| `/platform` | curl -sI | `HTTP/1.1 200 OK` |
| `/curriculum` | curl -sI | `HTTP/1.1 200 OK` |
| `/sitemap.xml` | curl -sI | `HTTP/1.1 200 OK` |

### A2 og.png

| 항목 | 방법 | 결과 |
|---|---|---|
| 라이브 응답 | curl -sI | 200 · `image/png` |
| 크기 | curl 실측 | 25,250 B (1200×630) |
| 렌더 도구 | Pillow 12.1.1 + Google Fonts woff2 | 브랜드 서체 그대로 |
| 글리프 확인 | 이미지 육안 | 1차 시도에서 「본」이 두부(□) → 글자별 서브셋으로 재렌더 후 정상 |
| 3페이지 메타 | grep | og:image·width·height·alt + twitter:image, card=summary_large_image |

### A3 MEDIA 빈 배열

| 항목 | 방법 | 결과 |
|---|---|---|
| `MEDIA.length` | 브라우저 | 0 |
| `#record` hidden | 브라우저 | `true` |
| computed display | 브라우저 | `none` |
| 차지 높이 | `getBoundingClientRect` | 0px |
| 부모 섹션(`#fakereal`) | 브라우저 | 정상 표시 |

> 항목이 **있을 때**의 렌더는 이전 세션에 테스트 항목 6개를 임시 투입해 확인했으나, 이번 세션에서는 실주소가 없어 재검증하지 않았습니다.

### B1 키보드

| 항목 | 방법 | 결과 |
|---|---|---|
| 포커스 가능 요소 | DOM 열거 | index 12(메뉴 닫힘) / 15(열림) |
| 양수 tabindex | DOM | 0건 |
| 포커스 트랩 | 메뉴 열고 Tab 이동 | 없음 |
| DOM 순서 vs 시각 순서 | Y좌표 비교 | 불일치 0건 |
| `aria-expanded` | 클릭 전후 비교 | false→true→false 정상 |
| **Escape로 닫기** | 키 이벤트 | **수정 전 미동작 → 수정 후 닫힘 + 포커스 토글 복귀** |

### B2 스크린리더

| 항목 | 방법 | 수정 전 | 수정 후 |
|---|---|---|---|
| 랜드마크 | DOM | header/nav/main/footer 각 1 | 동일 |
| `.pnum` 장식 번호 | DOM | 8건 낭독됨 | `aria-hidden="true"` 8/8 |
| tabpanel 포커스 | DOM | tabindex 없음 | `tabindex="0"` 2곳 |
| 링크 문구 중복 | 목적지 비교 | 「폼 작성 →」 2개가 다른 폼 | `.sr-only`로 목적지 구분 |
| 새 창 안내 | DOM | 전부 있음 | 동일 |
| 폼 라벨 | DOM | 입력 요소 없음 | — |

### B3 확대 리플로우 (WCAG 1.4.10)

| 뷰포트 | 방법 | index | platform | curriculum |
|---|---|---|---|---|
| 640px (200%) | `scrollWidth` 비교 | 가로 스크롤 없음 · 넘침 0 | — | — |
| 320px (400%) | 동일 | 없음 · 0 | 없음 · 0 | 없음 · 0 |

### B4 텍스트 간격 (WCAG 1.4.12)

line-height 1.5 / letter-spacing .12em / word-spacing .16em / p margin 2em 강제 적용.

| 페이지 | 방법 | 실질 잘림 | 가로 스크롤 |
|---|---|---|---|
| index | CSS 주입 후 `scrollHeight` 비교 | 0 | 없음 |
| platform | 동일 | 0 | 없음 |
| curriculum | 동일 | 0 | 없음 |

> `.sr-only`의 1×1 클립 4건은 의도된 구현이라 제외했습니다.

### C1 전송량 (실측)

| 페이지 | 무압축 | gzip | brotli | 절감 |
|---|---|---|---|---|
| `/` | 33,584 B | 10,203 B | 10,665 B | 68% |
| `/platform` | 23,271 B | 7,698 B | 7,887 B | 66% |
| `/curriculum` | 30,667 B | 8,440 B | 8,539 B | 72% |

> **gzip이 brotli보다 작습니다** (10,203 < 10,665). Cloudflare의 brotli 압축 수준이 이 크기대에서는 gzip보다 불리하게 나옵니다. 브라우저는 br을 우선 협상하므로 실사용은 br 값입니다.

폰트 (cold load, unicode-range 대조 + `Content-Length` 합산):

| 페이지 | 사용 문자 | 필요 face | 폰트 전송량 |
|---|---|---|---|
| index | 272자 | 92개 | 1,381.0 KB |
| platform | 193자 | 86개 | 1,343.7 KB |
| curriculum | 304자 | 122개 | 1,780.4 KB |

폰트 CSS 자체 79,819 B / `@font-face` 576개 선언.
**HTML은 10KB인데 폰트가 1.3~1.8MB — 전송량의 99%가 폰트입니다.**

> 브라우저 Resource Timing으로는 폰트가 캐시돼 0KB로 나와 측정 불가였습니다. 위 값은 필요한 서브셋을 판별해 실제 파일 크기를 합산한 것입니다.

### C2 Critical CSS (계산)

| 항목 | 방법 | 결과 |
|---|---|---|
| 전체 CSS 규칙 | `styleSheets` 열거 | 155개 |
| 첫 화면 사용 규칙 | 선택자별 뷰포트 내 매칭 | 43개 (**28%**) |
| 전체 CSS 문자 | 동일 | 16,119자 |
| 첫 화면 사용 문자 | 동일 | 4,841자 (**30%**) |
| 분리 시 예상 이득 | **추정** | HTML 인라인 CSS를 ~11KB 줄여 초기 파싱 감소. 단 brotli 후 실이득은 **2~3KB (추정)** — 폰트 1.4MB 대비 무의미 |

**판단**: 구현 가치 없음. 인라인 CSS는 이미 별도 요청이 없고, 병목은 폰트입니다.

### C3 폰트 로딩

| 항목 | 방법 | 결과 |
|---|---|---|
| `font-display` | 폰트 CSS 파싱 | **swap** (576/576 face) |
| preconnect | HTML grep | 2개 (googleapis, gstatic) |
| preload | HTML grep | 0개 |
| FOUT 폭 차이 | 웹폰트 vs 대체 폰트 실측 | h1 −4.9% · 본문 −6.3% · 워드마크 −9.6% |

`swap`이므로 텍스트는 즉시 표시되고 폰트 도착 시 교체됩니다(FOIT 없음). 폭 차이가 최대 −9.6%라 교체 시 미세한 재배치가 있으나 레이아웃 시프트는 텍스트 폭 안에서 흡수됩니다.
preload는 추가하지 않았습니다 — CSS가 `<head>` 앞부분에 있어 이미 조기 발견되고, preconnect로 연결 비용은 이미 절감됩니다.

### C4 LCP 요소

| 순위 | 요소 | 면적 | 폰트 크기 |
|---|---|---|---|
| 1 | `p.wordmark` (BON & GYEOL) | 32,159 px² | 43.8px |
| 2 | `h1` | 29,232 px² | 29.2px |
| 3 | `h2.reveal` | 23,986 px² | 23.2px |

이미지가 없으므로 LCP는 **텍스트 블록**입니다. 가장 큰 면적은 히어로 워드마크이고, Cormorant Garamond 로드 전에는 대체 폰트로 즉시 그려집니다.

> LCP API(`largest-contentful-paint`)는 옵서버 부착 시점이 페인트 이후여서 값을 받지 못했습니다. 위는 면적 계산으로 특정한 **후보**입니다.

### D 콘텐츠 정합성

| 항목 | 방법 | 결과 |
|---|---|---|
| D1 금지어 (자란다/무늬/뿌리/결실/아니라 등 9개) | 4페이지 본문 정규식 | **0건** |
| D1 회사 주어 헤드 / 캐치+설명 혼합 | h1·h2 패턴 검사 | **0건** |
| D2 없는 실적 (수강생·참여자·발매·수상·만족도) | 정규식 7종 | **0건** |
| D3 수강료·개강일·정원 | 정규식 6종 | curriculum 2건 → **"확정되지 않았습니다"·"모집 공지에서 안내" 문장이라 위반 아님** |
| D4 용어 일관성 | 전체 카운트 | 스태프 11 / 스탭 0 · 탑라인 7 / 탑라이너 0 · 디렉션 2 / 디렉팅 0 — 일관 |
| D5 JSON-LD | 파싱 + 본문 대조 | index url에 후행 슬래시 없었음 → 수정 |

> D5에서 하위 페이지의 `Organization.description`이 그 페이지 본문과 다른 것은 **정상**입니다. 조직 전체를 설명하는 필드이고 페이지 요약이 아닙니다. platform에 "12주"가, curriculum에 "매절"이 없는 것은 오류가 아닙니다.

### E1 링크 전수

| 분류 | 개수 | 결과 |
|---|---|---|
| 외부 | 9 | 7건 200 / **2건 404** |
| 내부 경로 | 8 | 전부 200 |
| mailto | 3 | 형식 정상 |
| 앵커 | 6 | 대상 존재 |

404 2건은 `https://fonts.googleapis.com` · `https://fonts.gstatic.com` 맨 오리진입니다. **`preconnect` 대상이라 정상** — 연결만 열고 가져오지 않습니다.

### E2 JS 비활성

`html.js` 클래스를 제거해 재현 (전이 없이 확정값이 나옴).

| 페이지 | 방법 | 투명 요소 | navLinks | navToggle |
|---|---|---|---|---|
| index | html.js 제거 | **0/30** | flex | none |
| curriculum | 동일 | **0/22** | flex | none |
| curriculum 탭 | 원본 HTML 확인 | `track-tabs`에 hidden 있음 / 두 패널 모두 hidden 없음 → 양쪽 트랙 노출 | | |
| platform 목록 | 소스 | JS 렌더 → `<noscript>` 안내문 유지 (폼은 정적이라 지원 가능) | | |

> 처음엔 `<noscript><style>`로 고쳤으나 스크립트가 켜진 상태에서는 검증할 수 없었습니다. `html.js` 게이팅으로 바꿔 **측정 가능한 구조**로 만들었습니다(`edc4d98`).

### E3 3G — 실패

브라우저 네트워크 스로틀 수단이 없어 실측하지 못했습니다. 실측 페이로드 기반 추정:

| 조건 | HTML 도착 | 폰트 전량 |
|---|---|---|
| 3G 느림 (400kbps, RTT 300ms) | ~1.1s (추정) | ~28.3s (추정) |
| 3G 보통 (780kbps, RTT 200ms) | ~0.7s (추정) | ~14.5s (추정) |
| 4G (4Mbps) | ~0.2s (추정) | ~2.8s (추정) |

`font-display:swap`이므로 폰트 도착 전에도 텍스트는 읽힙니다. 이미지가 없어 첫 화면 자체는 HTML 도착 직후 완성됩니다.

### E4 콘솔

| 페이지 | 방법 | 결과 |
|---|---|---|
| index | 새 탭 | 0건 |
| platform | 새 탭 | 0건 |
| curriculum | 새 탭 | 0건 |

### E5 HTML 유효성 (자체 구현)

| 항목 | 4페이지 결과 |
|---|---|
| DOCTYPE · lang · charset · title | 전부 정상 |
| 태그 열림/닫힘 균형 | 0건 |
| 중복 id · 중복 속성 | 0건 |
| void 태그 닫는 태그 | 0건 |
| h1 개수 | 각 1개 |

> "인용 없는 속성값" 1건은 `content="width=device-width…"` 인용 **안쪽**을 잘못 매칭한 자체 스크립트 오탐이었습니다.

### 파일 크기 (실측)

| 파일 | 크기 |
|---|---|
| index.html | 33,695 B |
| platform.html | 23,894 B |
| curriculum.html | 30,815 B |
| 404.html | 4,967 B |
| og.png | 25,250 B |
| _headers | 2,587 B |
| sitemap.xml | 598 B |
| favicon.svg | 640 B |

---

## 4. 변경 상세

| 항목 | 변경 | 커밋 | 검증 |
|---|---|---|---|
| DAW W6 | 「가사 작법」 → 「보컬 디렉션」 | `4b62c45` | 브라우저 12주 목록 |
| og.png | 1200×630 자체 렌더 + 3페이지 메타 + card=summary_large_image | `fe0dc61` | curl 200 image/png, 이미지 육안 |
| Escape 메뉴 | 3페이지 keydown 핸들러 + 포커스 복귀 | `c922de0` | 라이브 키 이벤트 |
| `.pnum` | aria-hidden 8곳 | `c922de0` | 라이브 DOM 8/8 |
| tabpanel | tabindex="0" 2곳 | `c922de0` | 라이브 DOM |
| door 링크 | sr-only로 목적지 구분 | `c922de0` | grep 2곳 |
| JSON-LD url | 후행 슬래시 추가 | `38cc347` | canonical 대조 |
| noscript → html.js | 기본 보임 + JS일 때만 효과 | `8196b82`→`edc4d98` | html.js 제거 재현 |
| 소개 여백 | `.dict{padding:90px 24px}` | `cd1a780` | 배포 후 curl 확인 |
| README | 카피 규칙·주의사항·MEDIA 표 | 이 커밋 | — |
| CLAUDE.md | 금지선·확정 결정·함정·검증 절차 | 이 커밋 | — |

---

## 5. 미해결·리스크

| # | 항목 | 원인 | 권장조치 |
|---|---|---|---|
| 1 | **폰트 1.3~1.8MB** | 한글 웹폰트 서브셋 분할. 전송량의 99% | 카피 안정 후 정확 서브셋 전환 (결정 2번) |
| 2 | 3G 실측 불가 | 스로틀 수단 없음 | 결정 1번 |
| 3 | MEDIA 항목 있을 때 미재검증 | 실주소 미확보 | 주소 전달 시 즉시 반영·검증 |
| 4 | 커리큘럼 12주 내용 미검증 | 문구를 내가 작성. 실제 강의 계획과 대조된 적 없음 | 두호가 대조 |
| 5 | Vol.08 장르 표기 | 「국악 크로스오버」는 추정값 | 확정 시 `PROJECTS` 수정 |
| 6 | LCP를 API로 측정 못 함 | 옵서버 부착이 페인트 이후 | 면적 계산 후보로 대체. 필요시 스크립트를 head 최상단으로 |
| 7 | Analytics 대시보드 미확인 | RUM 전송은 확인, 집계 화면은 볼 수 없음 | 두호가 대시보드 확인 |
| 8 | `'unsafe-inline'` CSP | 인라인 CSS·JS 구조상 필요 | 현 구조 유지. 외부 스크립트 주입은 여전히 차단 |
| 9 | 자체 검사 도구의 오탐 | axe·W3C validator·Lighthouse 없이 직접 구현 | 이번에 오탐 3건(zero-size focusable, 인용 없는 속성값, 고정 px 폭) 확인. 결과를 그대로 신뢰하지 말고 문맥 확인 필요 |

---

## 6. 다음 세션 큐

| # | 항목 | 선행조건 |
|---|---|---|
| 1 | MEDIA 항목 입력 + 렌더 검증 | fakeREAL 영상·트랙 주소 |
| 2 | Vol.08 장르 확정 반영 | **두호 확인** |
| 3 | 커리큘럼 12주 문구를 실제 강의안과 대조 | **두호 확인** |
| 4 | Analytics 유입 데이터 확인 후 기록 | 두호가 대시보드 확인 |
| 5 | 폰트 서브셋 전환 | **두호 결정 2번** (권장은 보류) |
| 6 | 3G 실측 | **두호 결정 1번** |
