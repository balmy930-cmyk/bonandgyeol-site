# 작업 보고서 — 2026-08-12

배포 커밋: `996bb03` · 저장소 clean

> **검증 출처 표기**
> `[자체]` 직접 만든 검사기 — 픽스처로 신뢰도 확인한 것만 (검증 차수 병기)
> `[자체·미검증]` 직접 만든 검사기 — 픽스처 케이스가 없어 신뢰도 미확인
> `[외부]` 브라우저 엔진·curl 등 외부 도구가 산출한 값
> `[육안]` 사람이 화면을 보고 판단

---

## 1. 두호 결정 필요

| # | 항목 | 선택지 | 권장 | 근거 |
|---|---|---|---|---|
| 1 | **Analytics 대시보드 집계** | (a) 두호가 직접 확인 (b) 계속 보류 | **(a)** | 브라우저에서 전송(RUM 1건)은 확인. **집계 화면은 Cloudflare 로그인이 필요해 대행 불가** |
| 2 | 픽스처 「기대 외 검출」 6건 | (a) 픽스처 보완 (b) 현행 유지 | **(b)** | 검사기 오탐이 아니라 픽스처 자체의 진짜 위반. 검사기 정확도에 영향 없음 |
| 3 | **sitemap.xml 엣지 캐싱** | (a) 대시보드 Cache Rule 추가 (b) 현행 유지 | **(b)** | 598B 파일 1개, 크롤러만 읽음. 히트율 개선 폭이 사실상 0 |
| 4 | 검사기 미구현 규칙 | (a) 확장 (b) 현 범위 유지 | **(b) 당분간 유지** | ARIA 상태 정합성·포커스 순서·색만으로 정보 전달 등 미구현. 현재 사이트 구조에선 해당 패턴이 없음 |

---

## 2. 요약

| # | 항목 | 상태 | 커밋 |
|---|---|---|---|
| 4 | Analytics 대시보드 수집 확인 | ⚠️ 부분 — 전송은 확인, 집계는 로그인 필요로 불가 | — |
| 1 | 검사기 신뢰도 검증 (테스트 8종) | ✅ 미커버 3건 보강 후 17/17 | `481dd64` |
| 1-b | 검사기 결함 수정 | ✅ 누적 3건 (오탐 2·미탐 1) | `6e73a41` `481dd64` |
| 2 | 검증 출처 표기 | ✅ 이 문서 전체에 적용 | 이 커밋 |
| 3 | JS-off 회귀 절차 | ✅ CLAUDE.md 명문화 | `6e73a41` |
| 5 | MEDIA 더미 4종 렌더 | ✅ iframe 격리 검증, 파일 무변경 | 커밋 없음 (지시대로) |
| 6 | 맥락 없는 링크 문구 | ✅ 0건 | — |
| 7 | **정적 자산 캐시 정책** | ✅ og.png REVALIDATED→HIT | `996bb03` |
| 7-b | sitemap.xml 엣지 캐싱 | ❌ 실패 — `_headers`만으로는 불가 (5절 2번) | — |

### 검사기에서 찾은 결함 (누적 3건)

| # | 결함 | 종류 | 실제 영향 |
|---|---|---|---|
| 1 | 로빙 탭인덱스를 위반으로 판정 | **오탐** | curriculum에서 실제 오탐 발생 중이었음 |
| 2 | `vague-link-text` 공백 정규화 누락 | **미탐** | `'자세히 보기'`가 매칭 안 돼 규칙이 반만 작동 |
| 3 | 인용부호 **안쪽**의 `=`를 인용 없는 값으로 판정 | **오탐** | 4페이지 전부에서 매번 1건씩 허위 보고 |

---

## 3. 검증

### 필수 확인

| 항목 | 방법 | 결과 |
|---|---|---|
| `bonandgyeol.com` | `[외부]` curl -sI | `HTTP/1.1 200 OK` |
| `/platform` | `[외부]` curl -sI | `HTTP/1.1 200 OK` |
| `/curriculum` | `[외부]` curl -sI | `HTTP/1.1 200 OK` |
| `/sitemap.xml` | `[외부]` curl -sI | `HTTP/1.1 200 OK` |

### 1. 검사기 신뢰도 — 지시된 테스트 8종

| 테스트 케이스 | 픽스처 반영 | 방법 | 탐지 |
|---|---|---|---|
| 대비 3.0 미달 텍스트 | 1.5:1 큰 텍스트 (28px/700) | `[자체]` `__a11y({expect:true})` | ✅ |
| 대비 4.5 미달 (본문) | 2.0:1 16px | 동일 | ✅ |
| **32px 클릭 타깃** | **이번에 추가** (링크·버튼 각 1) | 동일 | ✅ |
| label 없는 input | input + select | 동일 | ✅ |
| alt 없는 img | 1건 | 동일 | ✅ |
| 중복 id | `dupId` ×2 | 동일 | ✅ |
| h2 → h4 헤딩 건너뜀 | 1건 | 동일 | ✅ |
| **tabindex="-1" 버튼** | **이번에 추가** (tablist 밖 독립 버튼) | 동일 | ✅ |
| **인용 없는 속성값** | **이번에 추가** (`<p class=unquoted>`) | `[자체]` `python tools/html-check.py` | ✅ |

지시된 8종 중 **3종이 미커버**였고 이번에 추가했습니다. 미탐지 항목은 최종적으로 **0건**입니다.

### 1-b. 픽스처 대조 차수별 추이

| 차수 | 기대 위반 | 탐지 | 미탐지 | 음성 케이스 | 오탐 | 방법 |
|---|---|---|---|---|---|---|
| 1차 | 12 | 12 | 0 | — | — | `[자체]` |
| 2차 (음성 추가) | 12 | 12 | 0 | 7 | 0 | `[자체]` |
| 3차 (vague 규칙 추가) | 14 | 13 | **1** | 9 | 0 | `[자체]` |
| 4차 (오탐·미탐 수정) | 14 | 14 | 0 | 9 | 0 | `[자체]` |
| **5차 (3케이스 보강)** | **17** | **17** | **0** | **9** | **0** | `[자체]` |

> 음성 케이스(**잡히면 안 되는 것**) 9건이 핵심입니다. 없으면 "전부 잡는 검사기"가 100%를 받아 무의미해집니다.
> 음성 목록: `sr-only` · `hidden` 하위 2건 · `aria-hidden` SVG · `role=button`+`tabindex=0` · 같은 목적지 동일 문구 2건 · sr-only로 맥락 보강된 링크 · 로빙 탭인덱스

### 1-c. HTML 검사기 (양성·음성 동시 확인)

| 대상 | 방법 | 결과 |
|---|---|---|
| 픽스처 `<p class=unquoted>` | `[자체]` `html-check.py` | **탐지** (`unquoted-attr`) |
| 픽스처 중복 id | 동일 | 탐지 |
| 실제 4페이지 | 동일 | **전부 통과 (0건)** |
| `content="width=device-width, initial-scale=1.0"` | 동일 | **오탐 없음** (이전 판은 4페이지 전부 오탐) |

### 1-d. 수정 후 실제 페이지 재검사

| 페이지 | 방법 | 위반 | 내용 |
|---|---|---|---|
| index | `[자체]` `__a11y()` | 2 | `span.amp` · `span.ko-accent` 각 4.40:1 — 로고타입 예외 |
| platform | 동일 | 1 | `span.amp` 4.40:1 — 로고타입 예외 |
| curriculum | 동일 | 1 | `span.amp` 4.40:1 — 로고타입 예외 |

### 2. 검증 출처 구분

| 검증 항목 | 출처 |
|---|---|
| URL 상태코드 · 응답 헤더 · 전송량 | `[외부]` curl |
| 대비·클릭영역·라벨·헤딩·키보드·링크문구 | `[자체]` (픽스처 5차 검증) |
| HTML 유효성 | `[자체]` (양성·음성 확인) |
| JS-off 회귀 | `[자체]` (픽스처 없음 → 원리상 직접 재현: `html.js` 제거 후 계산값) |
| 레이아웃·뷰포트·그리드 | `[외부]` 브라우저 `getBoundingClientRect`·`getComputedStyle` |
| MEDIA 렌더 | `[외부]` 브라우저 실제 렌더 결과 |
| beacon·RUM 전송 | `[외부]` Resource Timing API |
| **Analytics 집계** | **미확인 — 대시보드 접근 불가** |
| 카피 금지어·실적·가격 | `[자체·미검증]` 정규식. 픽스처 케이스 없음 |
| 폰트 용량 | `[외부]` `Content-Length` 합산 + `[자체·미검증]` unicode-range 매칭 로직 |

### 3. JS-off 회귀

| 페이지 | 방법 | reveal | JS-off 시 투명 | navLinks | navToggle | 판정 |
|---|---|---|---|---|---|---|
| index | `[자체]` `__jsoff()` | 30 | **0** | flex | none | 통과 |
| platform | 동일 | 15 | **0** | flex | none | 통과 |
| curriculum | 동일 | 22 | **0** | flex | none | 통과 |

CLAUDE.md 검증 절차에 상시 항목으로 등재 (`6e73a41`).

### 4. Analytics

| 항목 | 방법 | 결과 |
|---|---|---|
| beacon 스크립트 요청 | `[외부]` Resource Timing | 1건 |
| RUM 전송 (`/cdn-cgi/rum`) | 동일 | 1건 |
| `__cfBeacon` 토큰 | `[외부]` JS | 존재 |
| CSP 차단 오류 | `[외부]` 콘솔 (새 탭) | 0건 |
| **대시보드 집계** | — | **확인 불가** |

전송 경로는 정상입니다. 집계 화면만 확인이 남았습니다 — 결정 1번.

### 5. MEDIA 더미 4종

`[외부]` 브라우저 렌더. **iframe 안에서만 주입해 파일을 전혀 수정하지 않았습니다** (되돌릴 것 없음).

| kind | 렌더 | 호스트 | 속성 |
|---|---|---|---|
| youtube | iframe | `www.youtube-nocookie.com` | title·lazy·referrerpolicy / `16/9` h=239px |
| soundcloud | iframe | `w.soundcloud.com` | 동일 / `auto` h=166px |
| instagram | 링크 카드 | `www.instagram.com` | `_blank`·`noopener`·44px·sr-only |
| x | 링크 카드 | `x.com` | 동일 |

외부 위젯 스크립트 **0개**. `git diff --quiet` 통과 (사이트 HTML 무변경).

### 6. 링크 문구

| 항목 | 방법 | 결과 |
|---|---|---|
| 맥락 없는 문구 12종 (자세히/보기/더보기/여기/클릭 등) | `[자체]` (픽스처 검증됨) | **0건** |
| 같은 문구·다른 목적지 | 동일 | 0건 |

### 7. 캐시 정책 (Zone Analytics 1.27% 대응)

**적용 전후 `cf-cache-status`** — `[외부]` curl -sI, 각 3회 요청 후 판정

| 경로 | 적용 전 | 적용 후 | Cache-Control (적용 후) | 판정 |
|---|---|---|---|---|
| `/` | DYNAMIC | DYNAMIC | `max-age=0, must-revalidate` | 의도된 결과 |
| `/platform` | DYNAMIC | DYNAMIC | 동일 | 의도된 결과 |
| `/curriculum` | DYNAMIC | DYNAMIC | 동일 | 의도된 결과 |
| **`/og.png`** | **REVALIDATED** | **HIT** | `max-age=2592000` | **개선** |
| `/favicon.svg` | HIT | HIT | 오리진 `2592000` / 캐시본 `604800` | 개선(반영 대기) |
| `/robots.txt` | HIT | HIT | `max-age=86400` | 유지 |
| `/sitemap.xml` | DYNAMIC | DYNAMIC | `max-age=86400` | **변화 없음** |

**원인 분석**

| 항목 | 방법 | 결과 |
|---|---|---|
| `/og.png`에 규칙 없었음 | `[자체]` `_headers` 확인 | Pages 기본값 `max-age=14400`이 적용되고 있었음 |
| HTML이 DYNAMIC인 이유 | `[외부]` Cloudflare 문서 + curl | `max-age=0, must-revalidate`는 **Pages의 기본값**이기도 함. 요청 대부분이 HTML이므로 히트율 상한 자체가 낮음 |
| 1.27%가 "비정상"인가 | 위 두 항목 종합 | **아님.** HTML을 캐시하지 않기로 한 정책의 산술적 결과 |
| `/favicon.svg` 헤더 미반영 | `[외부]` 쿼리로 캐시 우회 비교 | 캐시본 `604800` / 오리진 `2592000` — 기존 엣지 항목이 만료(최대 7일)되거나 purge해야 새 헤더 적용 |
| `/sitemap.xml` 엣지 미캐시 | `[외부]` 캐시 우회해도 DYNAMIC, `content-type: application/xml` | Cloudflare 엣지가 기본 캐시 대상으로 보지 않음. `_headers`로는 강제 불가 — 대시보드 Cache Rules 필요 |
| HTML 즉시 반영 유지 | `[외부]` `age` 헤더 | `age: 0` — 엣지 캐시에서 나오지 않음. 배포 즉시 반영 유지됨 |

**엣지 캐싱을 켜지 않은 이유** — `CDN-Cache-Control`/`s-maxage`로 HTML을 엣지 캐싱하면 히트율은 오르지만 배포 후 옛 페이지가 남을 수 있습니다. Cloudflare 문서도 *"커스텀 도메인에서의 커스텀 캐싱은 stale asset 문제를 만들 수 있으니 피하라"* 고 명시합니다. 지시하신 우선순위(배포 즉시 반영)와도 맞지 않아 적용하지 않았습니다.

---

## 4. 변경 상세

| 항목 | 변경 | 커밋 | 검증 |
|---|---|---|---|
| 캐시 정책 | `/og.png` 규칙 신설(30일), `/favicon.svg` 7일→30일. HTML은 유지 | `996bb03` | 적용 전후 `cf-cache-status` 실측 |
| 픽스처 보강 | 32px 타깃 ×2, 독립 `tabindex="-1"` 버튼, 인용 없는 속성값 + 음성 케이스 | `481dd64` | 5차 17/17 |
| HTML 검사기 | scratchpad → `tools/html-check.py`. 속성 토큰화로 재작성 | `481dd64` | 양성 1·음성 4페이지 |
| CLAUDE.md | HTML 검사기와 신뢰도 확인 방법 추가 | `481dd64` | — |
| (이전) a11y 검사기 | 로빙 탭인덱스 예외 + vague-link 정규화 | `6e73a41` | 4차 14/14 |
| (이전) JS-off 검사기 | `tools/jsoff-check.js` | `6e73a41` | 3페이지 통과 |

---

## 5. 미해결·리스크

| # | 항목 | 원인 | 권장조치 |
|---|---|---|---|
| 1 | Analytics 집계 미확인 | 대시보드 로그인 필요 | 결정 1번 |
| 2 | **sitemap.xml 엣지 미캐시** | `.xml`을 Cloudflare 엣지가 기본 캐시 대상으로 보지 않음. `_headers`로는 강제 불가 | 대시보드 Cache Rules 필요. 파일 1개·598B·크롤러 전용이라 실익 작음 → 결정 4번 |
| 3 | **검사기 미구현 규칙** | ARIA 상태 정합성, 포커스 순서 대 시각 순서, 색만으로 정보 전달, 자동재생, 세션 타임아웃 등 | 결정 3번. 해당 패턴이 생기면 규칙+픽스처 케이스를 함께 추가 |
| 4 | **카피·폰트 검사는 픽스처 없음** | 정규식·매칭 로직의 신뢰도를 확인하지 않음 | 3절에 `[자체·미검증]`으로 표기. 필요 시 픽스처 확장 |
| 5 | 픽스처가 합성 케이스 | 실제 페이지의 새 패턴을 자동으로 반영하지 못함 | 새 UI 패턴 도입 시 픽스처에도 추가 |
| 6 | 폰트 1.3~1.8MB | 한글 서브셋 분할. 전송량의 99% | 확정대로 현행 유지 |
| 7 | 커리큘럼 12주 내용 미검증 | 문구를 내가 작성. 실제 강의안과 대조된 적 없음 | 두호가 대조 |
| 8 | Vol.08 장르 표기 | 「국악 크로스오버」는 추정값 | 확정 시 `PROJECTS` 수정 |
| 9 | MEDIA 실항목 미투입 | 영상·트랙 주소 미확보 | 렌더는 더미로 검증 완료. 주소만 있으면 반영 |

---

## 6. 다음 세션 큐

| # | 항목 | 선행조건 |
|---|---|---|
| 1 | MEDIA 실항목 입력 | fakeREAL 영상·트랙 주소 |
| 2 | Analytics 집계 확인 후 기록 | **두호 대시보드 확인** |
| 3 | Vol.08 장르 확정 반영 | **두호 확인** |
| 4 | 커리큘럼 12주 문구를 실제 강의안과 대조 | **두호 확인** |
| 5 | 카피 검사기에 픽스처 케이스 추가 (`[자체·미검증]` 해소) | 없음 |
