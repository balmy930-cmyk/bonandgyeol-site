# 작업 리포트

최종 갱신: 2026-08-09 · 저장소: bonandgyeol-site

---

# 3차 — 자율 정비 (배포 대기 중 단독 수행 가능한 작업)

사용자 개입(계정·토큰·도메인)이 필요 없는 항목만 선별해 실행.

## 요약

| 구분 | 항목 |
|---|---|
| 완료 | 네비 버그 수정 · favicon · 404 · 보안헤더 · 워크플로 보강 · 전수 링크검사 |
| 미완 | og:image (래스터 이미지 생성 도구 없음) |
| 스킵 | 폰트 굵기 축소 (디자인 변경 수반 — 임의 판단 보류) |
| 차단 | Lighthouse 실측 · 배포 (Cloudflare 설정 필요) |

## 작업별 상세

| 항목 | 변경 내용 | 커밋 | 상태 |
|---|---|---|---|
| **네비 불일치** | platform에 커리큘럼 누락, curriculum에 오프라인·케어 누락 → 3개 페이지 7개 항목 통일 | `346df31` | 완료 |
| favicon | 대나무 마크 SVG 자체 호스팅, 3개 페이지 연결 | `73a4ccd` | 완료 |
| 404 페이지 | Pages가 자동 사용. 동일 팔레트, noindex, 바로가기 5개 | `f72e717` | 완료 |
| 보안 헤더 | CSP·HSTS·nosniff·frame DENY·Referrer·Permissions + 캐시 정책 | `af8cad8` | 완료 |
| 워크플로 보강 | `_headers`·`_redirects`는 확장자가 없어 허용목록에 안 걸림 → 명시 복사 | `af8cad8` | 완료 |

## 측정 결과

### 전수 검사 (Python 스크립트, 3개 페이지)

| 검사 항목 | 결과 |
|---|---|
| 깨진 내부 링크 | **0건** |
| 존재하지 않는 앵커 | **0건** |
| 블록 태그 열림/닫힘 불일치 | **0건** |
| h1 개수 (페이지당 1개) | 정상 |
| 텍스트 없는 링크 | 0건 |
| 44px 미만 클릭 영역 | **0건** (404 포함 4개 페이지) |

### CSP 적용 상태로 실제 서빙 검증

로컬 서버에 `_headers`의 CSP를 실제 응답 헤더로 적용해 확인.

| 항목 | 결과 |
|---|---|
| CSP 위반 | **0건** |
| Google Fonts 로드 | 정상 (Cormorant Garamond 적용 확인) |
| 인라인 CSS | 정상 |
| 인라인 JS (프로젝트 카드 렌더) | 정상 — 카드 1개·역할칩 3개 생성 |
| data: URI 그레인 배경 | 정상 |

### 로컬 성능 (참고치 — 실환경 아님)

| 지표 | 값 |
|---|---|
| DOM 노드 | 198개 (매우 적음) |
| FCP / LCP | 96ms (localhost 기준) |
| 폰트 요청 | **73건** (fonts.gstatic.com) |

> ⚠️ localhost 측정이라 실제 사용자 환경을 대변하지 않습니다. 배포 후 실제 URL에서 Lighthouse 측정 필요.

## 미해결·리스크

| 항목 | 원인 | 권장 조치 |
|---|---|---|
| **Cloudflare 미설정** | 프로젝트·Secrets 미생성 | [DEPLOY.md](DEPLOY.md) 1~6단계. 이게 나머지 전부의 선행 조건 |
| Lighthouse 미측정 | 배포 URL 없음 | 배포 후 측정 |
| 폰트 요청 73건 | 한글 웹폰트 유니코드 서브셋 분할 | 배포 후 실측 → 필요시 굵기 축소 검토 (300/400/500/600 전부 사용 중이라 디자인 영향 있음) |
| og:image 없음 | 래스터 이미지 생성 도구(PIL·node) 없음 + "외부 이미지 금지" 규칙과 충돌 | 규칙 해석 확정 후 1200×630 PNG 제작 |
| CSP `'unsafe-inline'` | CSS·JS를 HTML에 직접 넣는 구조 | 현 구조에선 적절. 해시 방식은 수정할 때마다 깨짐 |
| 임베드 URL 없음 | 음원 미공개 | `PROJECTS[0].embed`에 `{type,src}` 삽입 |
| 커리큘럼 12주 내용 | 브랜드 톤 기반 초안 | 실제 강의 계획과 대조 필요 |
| 장르 "국악 크로스오버" | 추정값 | 확정 시 수정 |

## 다음 작업 후보

1. **Cloudflare 배포 설정** — [DEPLOY.md](DEPLOY.md) 따라 1~6단계. 선행 조건
2. **배포 후 Lighthouse 4개 지표 측정** — 폰트 73건의 실제 영향 확인
3. **og:image 정책 확정 후 제작** — 카톡·X 공유 카드

---

# 2차 — 플랫폼 임베드 · 커리큘럼 · 접근성 · SEO

## 작업별 상세

| 항목 | 변경 내용 | 커밋 | 상태 |
|---|---|---|---|
| 임베드 구조 | `{type,src}` 객체 + `embedFrame()`, 16:9·title·lazy·referrerpolicy | `733b6c8` | 완료 |
| 커리큘럼 | 4단계 × 3주, 정적 마크업, CARE 모듈 3·10·11주 | `e26e089` | 완료 |
| index 연결 | 네비·카드에서 두 페이지 연결 | `1daa183` | 완료 |
| 접근성 | 대비·클릭영역·모션·JS 폴백 | `36a0101` | 완료 |
| SEO | Organization JSON-LD 3개 페이지, sitemap 3 URL | `b4a8126` | 완료 |
| 배포 안전성 | `.assetsignore` 의존 제거 → 허용목록 스테이징 | `fce532a` | 완료 |
| 배포 가이드 | DEPLOY.md 신규, README 재작성 | `64f679f` | 완료 |

## 명도 대비 (WCAG AA 4.5:1, 실측)

| 조합 | 이전 | 이후 |
|---|---|---|
| 본문 `--muted` | 4.41 ❌ | **5.19** ✅ |
| 소형 텍스트 (eyebrow·링크) | 4.40 ❌ | **6.59** ✅ |
| `--ink` on `--bg` | 13.25 | 13.25 (AAA) |
| 어두운 배경 보조 텍스트 | 4.51 | **7.45** ✅ |

## 이때 잡은 결함

| 결함 | 영향 |
|---|---|
| `--muted` 대비 4.41 | 본문 대부분이 WCAG AA 미달 |
| IntersectionObserver 폴백 없음 | 미지원 환경에서 페이지 대부분이 **영구 비표시** |
| `.assetsignore` Pages 지원 미확인 | README·REPORT가 사이트 루트에 공개될 위험 |

---

# 1차 — 플랫폼 페이지 신규

| 항목 | 커밋 | 상태 |
|---|---|---|
| platform.html 5섹션 신규 | `f3309f1` | 완료 |
| index 네비·카드 연결 | `245f0c0` | 완료 |
| sitemap 등록 | `6ace5a8` | 완료 |

## 규칙 준수 (전 차수 공통)

| 규칙 | 상태 |
|---|---|
| 단일 HTML·인라인 CSS/JS·외부 이미지 0 | ✅ (폰트 CDN만) |
| 팔레트 `#5C7348` / `#22231F` / `#EDEBE3` | ✅ 브랜드색 불변 |
| 서체 3종 고정 | ✅ |
| 클릭 영역 44px+ / focus-visible / 대비 / reduced-motion | ✅ |
| 없는 실적(참여자·수강생·성사 건수) 미표기 | ✅ |
| 커밋 분리 | ✅ |
