# 작업 리포트 — 플랫폼·커리큘럼 페이지 + 접근성·SEO

작업일: 2026-08-09 · 범위: `platform.html` 임베드 구조, `curriculum.html` 신규, 3개 페이지 접근성·SEO

## 요약

| 구분 | 항목 |
|---|---|
| 완료 | 임베드 구조 · curriculum.html · index 연결 · 접근성 4종 · JSON-LD/sitemap |
| 미완 | 실제 임베드 URL(음원 미공개), 커리큘럼 가격·일정(미확정, 의도적 보류) |
| 스킵 | Lighthouse 실측 — 로컬 정적 파일이라 서빙 불가. 배포 후 측정 필요 |

## 작업별 상세

| # | 항목 | 변경 내용 | 커밋 | 상태 |
|---|---|---|---|---|
| 1 | 임베드 구조 | `embed`를 `{type,src}` 객체로 전환, `embedFrame()` 생성 | `733b6c8` | 완료 |
| 1 | 임베드 규격 | 16:9 `aspect-ratio`, `title`, `loading="lazy"`, `referrerpolicy` | `733b6c8` | 완료 |
| 1 | 호스팅 | 파일 직접 호스팅 없음 — SoundCloud/YouTube 임베드만 | `733b6c8` | 완료 |
| 1 | 교체 위치 | `PROJECTS[0].embed: null` + 주석에 두 형식 예시 | `733b6c8` | 완료 |
| 2 | 커리큘럼 | 4단계 × 3주 = 12주, 정적 마크업(JS 비의존) | `e26e089` | 완료 |
| 2 | CARE 모듈 | 3·10·11주에 인라인 태그 + 전용 섹션(번아웃/거절/계약) | `e26e089` | 완료 |
| 2 | 금지 항목 | 가격·일정·정원·수강생 수 전부 미표기, "아직 정해지지 않은 것" 섹션으로 명시 | `e26e089` | 완료 |
| 3 | index 연결 | 네비 `온라인` → `플랫폼`·`커리큘럼` 페이지 링크로 교체 | `1daa183` | 완료 |
| 3 | 카드 연결 | 온라인 섹션 두 카드에 각 페이지 링크 | `1daa183` | 완료 |
| 4 | 명도 대비 | `--muted` 4.41 → 5.19, 소형 텍스트 `--bamboo` 4.40 → `--bamboo-deep` 6.59 | `36a0101` | 완료 |
| 4 | 클릭 영역 | `.door a` 36 → 44px, 푸터·노트 링크 44px | `36a0101` | 완료 |
| 4 | 모션 | reduced-motion에서 전 transition/animation 차단, 호버 이동 제거 | `36a0101` | 완료 |
| 4 | JS 폴백 | IntersectionObserver 없으면 `.reveal` 즉시 표시 (이전엔 영구 비표시) | `36a0101` | 완료 |
| 5 | JSON-LD | Organization 스키마 3개 페이지 삽입, 파싱 검증 | `b4a8126` | 완료 |
| 5 | sitemap | `/curriculum.html` 추가 (총 3개 URL) | `b4a8126` | 완료 |

## 측정 결과

### 명도 대비 (WCAG AA 기준 4.5:1, 실측)

| 조합 | 이전 | 이후 | 판정 |
|---|---|---|---|
| 본문 `--muted` on `--bg` | 4.41 ❌ | **5.19** | AA 통과 |
| eyebrow·링크 소형 텍스트 | 4.40 ❌ | **6.59** | AA 통과 |
| `--ink` on `--bg` | 13.25 | 13.25 | AAA |
| 흰색 on `--bamboo` | 5.25 | 5.25 | AA |
| `--bamboo-soft` on `--ink` | 5.80 | 5.80 | AA |
| 어두운 배경 보조 텍스트 | 4.51 | **7.45** | AA 통과 |

### 클릭 영역 (44px 미만 개수)

| 페이지 | 이전 | 이후 |
|---|---|---|
| index.html | 0 | 0 |
| platform.html | 2 ❌ | **0** |
| curriculum.html | — | **0** |

### 파일 크기 / 기타

| 항목 | 값 |
|---|---|
| index.html | 25,692 B (gzip 7,705 B) |
| platform.html | 22,143 B (gzip 7,335 B) |
| curriculum.html | 24,547 B (gzip 7,774 B) |
| 외부 이미지 | 0 (그레인은 인라인 SVG) |
| 콘솔 오류 | 3개 페이지 0건 |
| 320px 가로 스크롤 | 없음 (넘침 요소 0) |
| JSON-LD 파싱 | 3개 페이지 정상 |
| 헤딩 순서 | curriculum h1→h2→h3→h4 정상 |

## 미해결·리스크

| 항목 | 원인 | 권장 조치 |
|---|---|---|
| Lighthouse 4개 지표 미측정 | 로컬 파일은 서빙 환경 없음 | 배포 후 실제 URL에서 측정 |
| 임베드 URL 없음 | 음원 미공개 | `PROJECTS[0].embed`에 `{type,src}` 삽입 |
| 프로젝트 카드 JS 의존 | 카드가 JS 렌더 | 곡 수 늘면 정적 HTML 전환 검토 |
| 장르 "국악 크로스오버" | 지정값 아닌 추정 | 확정 시 배열에서 수정 |
| 커리큘럼 12주 내용 | 브랜드 톤 기반 초안 | 실제 강의 계획과 대조·확정 필요 |
| "결" 워드마크 대비 4.40 | 브랜드 색 고정 | WCAG 로고타입 예외 해당 — 조치 불필요 |
| OG 이미지 없음 | 외부 이미지 금지 | 공유 썸네일 필요 시 정책 결정 |
| Cloudflare 미설정 | 프로젝트·Secrets 미생성 | 배포 실패는 정상, 설정 후 재실행 |

## 다음 작업 후보

1. **Cloudflare 배포 설정 완료** — 프로젝트 생성 + Secrets 2건 등록 → 실제 URL 확보 → Lighthouse 측정
2. **fakeREAL Vol.08 음원 임베드 삽입** — SoundCloud/YouTube URL을 `PROJECTS[0].embed`에 주입
3. **커리큘럼 12주 내용 확정** — 초안을 실제 강의 계획과 대조, 1기 모집 공지 시 가격·일정 섹션 추가
